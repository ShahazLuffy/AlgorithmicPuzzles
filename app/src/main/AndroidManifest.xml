<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shahaz.algorithmicpuzzles">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/puzzle"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TempActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".moama_kolah_act" />
        <activity android:name=".notationact" />
        <activity android:name=".moama_sekkeh_act" />
        <activity android:name=".LoadDetails" />
        <activity android:name=".moama_chopan_act" />
        <activity android:name=".AllProductsActivity" />
        <activity android:name=".introact" />
        <activity android:name=".hintact" />
        <activity android:name=".list_puzzle" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".TestActivity"/>
    </application>

</manifest>